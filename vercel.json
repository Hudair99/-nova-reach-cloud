{
  "buildCommand": "npm run build",
  "outputDirectory": "build",
  "framework": "react",
  "rewrites": [
    {
      "source": "/:path((?!api/.*).*)",
      "destination": "/index.html"
    }
  ]
}
